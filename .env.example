# 数据库连接字符串
POSTGRES_URL=postgresql://user:password@localhost:5432/databasename

# GitHub OAuth 应用配置（可选）
# 需要在 GitHub 开发者设置中创建 OAuth App 获取
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# 更安全的做法是使用环境变量来存储敏感信息
# 在生产环境中，不要将真实的密钥存储在代码库中